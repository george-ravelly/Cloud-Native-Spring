
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO:https://github.com/george-ravelly/Config-repo}
          skip-ssl-validation: true
          search-paths: {application}
server:
  port: 8888
